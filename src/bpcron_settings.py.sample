# Log settings
import logging

fmt = '%(asctime)s - %(name)s - %(levelname)s - %(message)s'
logging.basicConfig(format=fmt, filename='bp-cron.log', level=logging.INFO)
# logging.basicConfig(format=fmt, level=logging.DEBUG)

# Slack settings

# Create token https://api.slack.com/docs/oauth-test-tokens
SLACK_TOKEN = '<your-slack-api-token>'

BOT_EMOJI = ':robot_face:'
BOT_NAME = 'bp-cron'

# デバッグモードありでチャンネルを指定すると、すべてのメッセージがここに流れる
# DEBUG = True
# DEBUG_CHANNEL = '#bot-test'
